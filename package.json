{
  "name": "soyanga",
  "private": true,
  "version": "1.0.0",
  "description": "Monorepo SOYANGA (backend + frontend)",
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND -c blue,green \"cd soyanga-backend && mvnw.cmd spring-boot:run\" \"npm --prefix frontend run dev\"",
    "dev:back": "cd soyanga-backend && mvnw.cmd spring-boot:run",
    "dev:front": "npm --prefix frontend run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
